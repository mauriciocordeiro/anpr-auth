{
    "configurations": [
        {
            "name": "Docker: CatEye ALPR",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "python": {
                "pathMappings": [
                    {
                        "localRoot": "${workspaceFolder}",
                        "remoteRoot": "/app"
                    }
                ],
                "port": 5000,
                "projectType": "general"
            }
        }
    ]
}